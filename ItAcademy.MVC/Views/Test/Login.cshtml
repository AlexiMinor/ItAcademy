@model LoginModel

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>Register</h2>
@* @for *@
@* @for (int i = 0; i < 1; i++)
{
    var x = 0;
    var y = 1;

    <div>
        <label asp-for="Email"></label>
        <input asp-for="Email" type="email" value="@Model?.Email"/>
    </div>
} *@
<form action="/test/login?password=123" method="post">
    <div>
        <label asp-for="Email"></label>
        <input asp-for="Email" type="email" value="@Model?.Email"/>
    </div>
    <div>
        <label asp-for="Password"></label>
        <input asp-for="Password" type="password" />
    </div>
    <button>Submit</button>
</form>